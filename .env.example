# === PRZYKŁADOWY PLIK KONFIGURACJI DLA ZEWNĘTRZNEGO DEPLOYMENTU ===
# Skopiuj ten plik jako .env i ustaw odpowiednie wartości
# NIGDY NIE COMMITUJ PLIKU .env DO REPOZYTORIUM!

# === BAZA DANYCH (WYMAGANE) ===
DATABASE_URL=postgresql://username:password@localhost:5432/equipment_rental

# === AUTENTYKACJA (WYMAGANE) ===
# Długi, losowy klucz do podpisywania JWT tokenów
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters-long
# Klucz do sesji (backup dla kompatybilności)
SESSION_SECRET=your-session-secret-here-also-long-and-random

# === KONFIGURACJA AUTENTYKACJI ===
# standard = email/hasło, replit = Replit OAuth (automatycznie wykrywane)
AUTH_PROVIDER=standard
# Czy pozwolić na rejestrację nowych użytkowników
ALLOW_REGISTRATION=true
# Czy nowi użytkownicy wymagają zatwierdzenia przez admina
REQUIRE_APPROVAL=true

# === ŚRODOWISKO ===
NODE_ENV=production
PORT=5000

# === CORS (Cross-Origin Resource Sharing) ===
# Domeny, które mogą się połączyć z API
CORS_ORIGIN=https://yourdomain.com
# Dla wszystkich domen (TYLKO DO ROZWOJU): CORS_ORIGIN=*

# === BEZPIECZEŃSTWO ===
# Włącz dodatkowe nagłówki bezpieczeństwa w produkcji
ENABLE_SECURITY_HEADERS=true

# === STORAGE PLIKÓW (opcjonalne) ===
# local = pliki lokalne, s3 = Amazon S3, gcs = Google Cloud Storage
OBJECT_STORAGE_PROVIDER=local

# Dla Amazon S3:
# OBJECT_STORAGE_PROVIDER=s3
# OBJECT_STORAGE_BUCKET=your-bucket-name
# OBJECT_STORAGE_REGION=eu-central-1
# OBJECT_STORAGE_ACCESS_KEY=your-aws-access-key
# OBJECT_STORAGE_SECRET_KEY=your-aws-secret-key

# Dla Google Cloud Storage:
# OBJECT_STORAGE_PROVIDER=gcs
# OBJECT_STORAGE_BUCKET=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# === DODATKOWE USTAWIENIA ===
# Maksymalny rozmiar uploadowanych plików (w MB)
MAX_FILE_SIZE=100

# Poziom logowania (error, warn, info, debug)
LOG_LEVEL=info

# Czy wyświetlać szczegółowe błędy (tylko development)
SHOW_ERROR_DETAILS=false